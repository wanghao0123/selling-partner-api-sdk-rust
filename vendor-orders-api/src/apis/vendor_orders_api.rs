/* 
 * Selling Partner API for Retail Procurement Orders
 *
 * The Selling Partner API for Retail Procurement Orders provides programmatic access to vendor orders data.
 *
 * OpenAPI spec version: v1
 * 
 * Generated by: https://github.com/swagger-api/swagger-codegen.git
 */

use std::rc::Rc;
use std::borrow::Borrow;
use std::borrow::Cow;
use std::collections::HashMap;

use hyper;
use serde_json;
use futures;
use futures::{Future, Stream};

use hyper::header::UserAgent;

use super::{Error, configuration};

pub struct VendorOrdersApiClient<C: hyper::client::Connect> {
    configuration: Rc<configuration::Configuration<C>>,
}

impl<C: hyper::client::Connect> VendorOrdersApiClient<C> {
    pub fn new(configuration: Rc<configuration::Configuration<C>>) -> VendorOrdersApiClient<C> {
        VendorOrdersApiClient {
            configuration: configuration,
        }
    }
}

pub trait VendorOrdersApi {
    fn get_purchase_order(&self, purchase_order_number: &str) -> Box<Future<Item = ::models::GetPurchaseOrderResponse, Error = Error<serde_json::Value>>>;
    fn get_purchase_orders(&self, limit: i64, created_after: String, created_before: String, sort_order: &str, next_token: &str, include_details: &str, changed_after: String, changed_before: String, po_item_state: &str, is_po_changed: &str, purchase_order_state: &str, ordering_vendor_code: &str) -> Box<Future<Item = ::models::GetPurchaseOrdersResponse, Error = Error<serde_json::Value>>>;
    fn get_purchase_orders_status(&self, limit: i64, sort_order: &str, next_token: &str, created_after: String, created_before: String, updated_after: String, updated_before: String, purchase_order_number: &str, purchase_order_status: &str, item_confirmation_status: &str, ordering_vendor_code: &str, ship_to_party_id: &str) -> Box<Future<Item = ::models::GetPurchaseOrdersStatusResponse, Error = Error<serde_json::Value>>>;
    fn submit_acknowledgement(&self, body: ::models::SubmitAcknowledgementRequest) -> Box<Future<Item = ::models::SubmitAcknowledgementResponse, Error = Error<serde_json::Value>>>;
}


impl<C: hyper::client::Connect>VendorOrdersApi for VendorOrdersApiClient<C> {
    fn get_purchase_order(&self, purchase_order_number: &str) -> Box<Future<Item = ::models::GetPurchaseOrderResponse, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/vendor/orders/v1/purchaseOrders/{purchaseOrderNumber}?{}", configuration.base_path, query_string, purchaseOrderNumber=purchase_order_number);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }




        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::GetPurchaseOrderResponse, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_purchase_orders(&self, limit: i64, created_after: String, created_before: String, sort_order: &str, next_token: &str, include_details: &str, changed_after: String, changed_before: String, po_item_state: &str, is_po_changed: &str, purchase_order_state: &str, ordering_vendor_code: &str) -> Box<Future<Item = ::models::GetPurchaseOrdersResponse, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.append_pair("limit", &limit.to_string());
            query.append_pair("createdAfter", &created_after.to_string());
            query.append_pair("createdBefore", &created_before.to_string());
            query.append_pair("sortOrder", &sort_order.to_string());
            query.append_pair("nextToken", &next_token.to_string());
            query.append_pair("includeDetails", &include_details.to_string());
            query.append_pair("changedAfter", &changed_after.to_string());
            query.append_pair("changedBefore", &changed_before.to_string());
            query.append_pair("poItemState", &po_item_state.to_string());
            query.append_pair("isPOChanged", &is_po_changed.to_string());
            query.append_pair("purchaseOrderState", &purchase_order_state.to_string());
            query.append_pair("orderingVendorCode", &ordering_vendor_code.to_string());
            query.finish()
        };
        let uri_str = format!("{}/vendor/orders/v1/purchaseOrders?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }




        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::GetPurchaseOrdersResponse, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_purchase_orders_status(&self, limit: i64, sort_order: &str, next_token: &str, created_after: String, created_before: String, updated_after: String, updated_before: String, purchase_order_number: &str, purchase_order_status: &str, item_confirmation_status: &str, ordering_vendor_code: &str, ship_to_party_id: &str) -> Box<Future<Item = ::models::GetPurchaseOrdersStatusResponse, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.append_pair("limit", &limit.to_string());
            query.append_pair("sortOrder", &sort_order.to_string());
            query.append_pair("nextToken", &next_token.to_string());
            query.append_pair("createdAfter", &created_after.to_string());
            query.append_pair("createdBefore", &created_before.to_string());
            query.append_pair("updatedAfter", &updated_after.to_string());
            query.append_pair("updatedBefore", &updated_before.to_string());
            query.append_pair("purchaseOrderNumber", &purchase_order_number.to_string());
            query.append_pair("purchaseOrderStatus", &purchase_order_status.to_string());
            query.append_pair("itemConfirmationStatus", &item_confirmation_status.to_string());
            query.append_pair("orderingVendorCode", &ordering_vendor_code.to_string());
            query.append_pair("shipToPartyId", &ship_to_party_id.to_string());
            query.finish()
        };
        let uri_str = format!("{}/vendor/orders/v1/purchaseOrdersStatus?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }




        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::GetPurchaseOrdersStatusResponse, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn submit_acknowledgement(&self, body: ::models::SubmitAcknowledgementRequest) -> Box<Future<Item = ::models::SubmitAcknowledgementResponse, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Post;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/vendor/orders/v1/acknowledgements?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }



        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::SubmitAcknowledgementResponse, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

}
