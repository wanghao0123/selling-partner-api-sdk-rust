/* 
 * Selling Partner API for Services
 *
 * With the Services API, you can build applications that help service providers get and modify their service orders.
 *
 * OpenAPI spec version: v1
 * 
 * Generated by: https://github.com/swagger-api/swagger-codegen.git
 */

use std::rc::Rc;
use std::borrow::Borrow;
use std::borrow::Cow;
use std::collections::HashMap;

use hyper;
use serde_json;
use futures;
use futures::{Future, Stream};

use hyper::header::UserAgent;

use super::{Error, configuration};

pub struct ServiceApiClient<C: hyper::client::Connect> {
    configuration: Rc<configuration::Configuration<C>>,
}

impl<C: hyper::client::Connect> ServiceApiClient<C> {
    pub fn new(configuration: Rc<configuration::Configuration<C>>) -> ServiceApiClient<C> {
        ServiceApiClient {
            configuration: configuration,
        }
    }
}

pub trait ServiceApi {
    fn add_appointment_for_service_job_by_service_job_id(&self, service_job_id: &str, body: ::models::AddAppointmentRequest) -> Box<Future<Item = ::models::SetAppointmentResponse, Error = Error<serde_json::Value>>>;
    fn cancel_service_job_by_service_job_id(&self, service_job_id: &str, cancellation_reason_code: &str) -> Box<Future<Item = ::models::CancelServiceJobByServiceJobIdResponse, Error = Error<serde_json::Value>>>;
    fn complete_service_job_by_service_job_id(&self, service_job_id: &str) -> Box<Future<Item = ::models::CompleteServiceJobByServiceJobIdResponse, Error = Error<serde_json::Value>>>;
    fn get_service_job_by_service_job_id(&self, service_job_id: &str) -> Box<Future<Item = ::models::GetServiceJobByServiceJobIdResponse, Error = Error<serde_json::Value>>>;
    fn get_service_jobs(&self, marketplace_ids: Vec<String>, service_order_ids: Vec<String>, service_job_status: Vec<String>, page_token: &str, page_size: i32, sort_field: &str, sort_order: &str, created_after: &str, created_before: &str, last_updated_after: &str, last_updated_before: &str, schedule_start_date: &str, schedule_end_date: &str) -> Box<Future<Item = ::models::GetServiceJobsResponse, Error = Error<serde_json::Value>>>;
    fn reschedule_appointment_for_service_job_by_service_job_id(&self, service_job_id: &str, appointment_id: &str, body: ::models::RescheduleAppointmentRequest) -> Box<Future<Item = ::models::SetAppointmentResponse, Error = Error<serde_json::Value>>>;
}


impl<C: hyper::client::Connect>ServiceApi for ServiceApiClient<C> {
    fn add_appointment_for_service_job_by_service_job_id(&self, service_job_id: &str, body: ::models::AddAppointmentRequest) -> Box<Future<Item = ::models::SetAppointmentResponse, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Post;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/service/v1/serviceJobs/{serviceJobId}/appointments?{}", configuration.base_path, query_string, serviceJobId=service_job_id);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }



        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::SetAppointmentResponse, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn cancel_service_job_by_service_job_id(&self, service_job_id: &str, cancellation_reason_code: &str) -> Box<Future<Item = ::models::CancelServiceJobByServiceJobIdResponse, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Put;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.append_pair("cancellationReasonCode", &cancellation_reason_code.to_string());
            query.finish()
        };
        let uri_str = format!("{}/service/v1/serviceJobs/{serviceJobId}/cancellations?{}", configuration.base_path, query_string, serviceJobId=service_job_id);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }




        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::CancelServiceJobByServiceJobIdResponse, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn complete_service_job_by_service_job_id(&self, service_job_id: &str) -> Box<Future<Item = ::models::CompleteServiceJobByServiceJobIdResponse, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Put;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/service/v1/serviceJobs/{serviceJobId}/completions?{}", configuration.base_path, query_string, serviceJobId=service_job_id);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }




        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::CompleteServiceJobByServiceJobIdResponse, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_service_job_by_service_job_id(&self, service_job_id: &str) -> Box<Future<Item = ::models::GetServiceJobByServiceJobIdResponse, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/service/v1/serviceJobs/{serviceJobId}?{}", configuration.base_path, query_string, serviceJobId=service_job_id);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }




        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::GetServiceJobByServiceJobIdResponse, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_service_jobs(&self, marketplace_ids: Vec<String>, service_order_ids: Vec<String>, service_job_status: Vec<String>, page_token: &str, page_size: i32, sort_field: &str, sort_order: &str, created_after: &str, created_before: &str, last_updated_after: &str, last_updated_before: &str, schedule_start_date: &str, schedule_end_date: &str) -> Box<Future<Item = ::models::GetServiceJobsResponse, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.append_pair("serviceOrderIds", &service_order_ids.join(",").to_string());
            query.append_pair("serviceJobStatus", &service_job_status.join(",").to_string());
            query.append_pair("pageToken", &page_token.to_string());
            query.append_pair("pageSize", &page_size.to_string());
            query.append_pair("sortField", &sort_field.to_string());
            query.append_pair("sortOrder", &sort_order.to_string());
            query.append_pair("createdAfter", &created_after.to_string());
            query.append_pair("createdBefore", &created_before.to_string());
            query.append_pair("lastUpdatedAfter", &last_updated_after.to_string());
            query.append_pair("lastUpdatedBefore", &last_updated_before.to_string());
            query.append_pair("scheduleStartDate", &schedule_start_date.to_string());
            query.append_pair("scheduleEndDate", &schedule_end_date.to_string());
            query.append_pair("marketplaceIds", &marketplace_ids.join(",").to_string());
            query.finish()
        };
        let uri_str = format!("{}/service/v1/serviceJobs?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }




        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::GetServiceJobsResponse, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn reschedule_appointment_for_service_job_by_service_job_id(&self, service_job_id: &str, appointment_id: &str, body: ::models::RescheduleAppointmentRequest) -> Box<Future<Item = ::models::SetAppointmentResponse, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Post;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/service/v1/serviceJobs/{serviceJobId}/appointments/{appointmentId}?{}", configuration.base_path, query_string, serviceJobId=service_job_id, appointmentId=appointment_id);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }



        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::SetAppointmentResponse, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

}
